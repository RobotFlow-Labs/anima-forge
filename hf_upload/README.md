# FORGE model card staging

Public model-card publishing is withheld until the final trained checkpoint, evaluation,
compression, and runtime artifacts pass the release gates. Do not add measurements here
independently; artifact-backed claims must first enter the repository's public claim
registry and release documentation.

See the repository `README.md` for installation and current supported-model information.
